{
  "name": "tuba-team",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && CI=false npm run build && cd ../backend && npm install --production",
    "start": "node backend/server.js"
  },
  "engines": { "node": ">=18.0.0" }
}
